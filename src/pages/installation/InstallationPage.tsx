import React from 'react';
import Datatable from '../Datatable/Datatable';

type Props = {};

const Sale = (props: Props) => {
  return (
    <div>
    
    <Datatable/>
    </div>
  );
};

export default Sale;